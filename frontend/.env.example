# ─── Frontend Environment ─────────────────────────────────────
# Optional: override the API base URL (defaults to http://localhost:3000/api)
# In production with Vite proxy, you can leave this unset.
VITE_API_BASE_URL=http://localhost:3000/api
